> [!NOTE]
> Documentation auto-generated by [ai-docs.](https://github.com/connor-john/ai-docs)

Okay, here's a significantly enhanced and more detailed technical documentation outline based on the
provided Flutter codebase, focusing on architecture, performance, security, and data flow. This is
structured for internal development handover.

**I. Project Overview**

* **Project Name:** SchoolMate
* **Description:** A cross-platform mobile application built with Flutter for managing
  school-related activities, including schedules, homework, grades, and notifications.
* **Target Audience:** Students, potentially teachers (future enhancement).
* **Technology Stack:** Flutter, Dart, Supabase, AndroidAlarmManager, various external APIs.

**II. System Architecture**

* **Diagram:**

  ```
  [Client (Flutter App)]
     | (HTTPS Requests/Responses)
     V
  [API Gateway (Conceptual)]  // Could be a dedicated service or part of Supabase Edge Functions
     | (Routes requests to appropriate services)
     V
  +---------------------------------------------------------------------+
  |                                                                     |
  |   [Supabase Backend]                                               |
  |   +---------------------------------------------------------------+ |
  |   |  [Authentication Service]                                     | |
  |   |   - User management (signup, signin, password reset)          | |
  |   |   - JWT token issuance and verification                       | |
  |   |   - Supabase Auth                                             | |
  |   +---------------------------------------------------------------+ |
  |   |  [PostgreSQL Database]                                        | |
  |   |   - Schedule data (lessons, subjects, teachers)                | |
  |   |   - Homework tasks                                            | |
  |   |   - Grade information (grading systems, marks)                | |
  |   |   - User settings (residence, notifications)                  | |
  |   +---------------------------------------------------------------+ |
  |   |  [Edge Functions (Optional)]                                    | |
  |   |   - Custom API logic, transformations, and integrations       | |
  |   +---------------------------------------------------------------+ |
  +---------------------------------------------------------------------+
     | (HTTP Requests)                                                  |
     V
  +---------------------------------------------------------------------+
  |  [External APIs]                                                    |
  |  - restcountries.com (Country data)                                 |
  |  - geonames.org (State/Sub-country data)                            |
  |  - openholidaysapi.org (School holiday data)                        |
  +---------------------------------------------------------------------+
     | (Scheduled Tasks via AndroidAlarmManager)                          |
     V
  [Background Task Handler (Flutter App)]
      - scheduleTaskCallback
      - schedulePreLessonNotificationsForCurrentDay
  ```

* **Component Descriptions:**

    * **Client (Flutter App):**  The user interface, handling user input, data display, and
      communication with the backend. Key responsibilities include authentication, data fetching,
      and UI rendering. Uses `provider` or `bloc` for state management (add this if it's used).
    * **API Gateway (Conceptual):** In this architecture, the Supabase backend *acts* as the API
      gateway. It handles routing requests to the appropriate services (e.g., authentication,
      database access). If complexity increases, consider using Supabase Edge Functions or a
      dedicated API Gateway service.
    * **Supabase Backend:**
        * **Authentication Service:** Manages user authentication using Supabase Auth. Handles
          signup, signin, password resets, and issues JWT (JSON Web Tokens) for secure API access.
        * **PostgreSQL Database:**  The core data store, structured with schemas
          for `schedule`, `homework`, `grades`, and `settings`.
        * **Edge Functions (Optional):** Serverless functions that can be used for custom API logic,
          data transformations, or integrations with other services. Useful for tasks that shouldn't
          be performed on the client.
    * **External APIs:** Integrations with third-party services for:
        * `restcountries.com`: Fetching country names, codes, and flag URLs.
        * `geonames.org`:  Retrieving state/sub-country information based on country codes. Requires
          a `GEONAMES_API_USER` environment variable.
        * `openholidaysapi.org`:  Fetching school holiday data based on location.
    * **Background Task Handler (Flutter App):** Uses `AndroidAlarmManager` to schedule tasks that
      run in the background, even when the app is closed. Specifically used for scheduling daily
      pre-lesson notifications.

* **Data Flow:**

    1. **User Interaction:** User interacts with the Flutter app (e.g., adds a homework task).
    2. **API Request:** The Flutter app sends an HTTPS request to the Supabase backend (via the
       conceptual API Gateway). The request includes a JWT for authentication.
    3. **Authentication:** Supabase Auth verifies the JWT. If the token is invalid, the request is
       rejected.
    4. **Data Access:**  The Supabase backend (typically using PostgreSQL) processes the request and
       retrieves or modifies data in the database.
    5. **External API Call (Conditional):**  For certain operations (e.g., fetching country data),
       the Supabase backend (or potentially an Edge Function) makes a request to an external API.
    6. **Response:** The Supabase backend sends an HTTPS response back to the Flutter app,
       containing the requested data or a confirmation message.
    7. **UI Update:** The Flutter app updates the UI to reflect the changes.
    8. **Background Tasks:** The `AndroidAlarmManager` triggers the `scheduleTaskCallback` function
       in the Flutter app at a scheduled time.
    9. **Notification Scheduling:** The `scheduleTaskCallback` fetches the user's schedule and
       pre-lesson notification settings from Supabase, calculates the notification times, and
       schedules local notifications using `flutterLocalNotificationsPlugin`.

* **Key Architectural Decisions:**

    * **Choice of Flutter:**  Cross-platform development, rapid UI development, and a rich ecosystem
      of libraries.
    * **Choice of Supabase:**  Backend-as-a-service (BaaS) that provides authentication, database,
      and serverless functions, simplifying backend development and deployment.
    * **Local Notifications:** Used `AndroidAlarmManager` for precise scheduling even in Doze mode,
      but requires user permission and careful battery optimization.
    * **External APIs:** Leveraged to enrich the application with geographic and holiday data.
    * **Dark Theme:** Use of themeMode and ThemeData to set the overall theme of the application.

**III. Codebase Insights**

* **Complex Modules:**

    * **`API/supabase/grades/gradingSystem.dart`:**
        * **Functionality:** Manages the grading system for each user, including defining grade
          ranges, modifiers (+/-), and exam types. Handles complex logic for creating, fetching, and
          validating grading systems.
        * **Implementation Details:**
            * The `setGradingSystem` function performs a series of database operations to clear
              existing grading system data and insert new data. It uses
              a `dbMultiplicationChildTypeCrossReferenceLookupTable` to handle the hierarchical
              relationships between exam types in multiplication-based grading systems.
            * The `fetchGradingSystem` function retrieves the grading system data and exam types
              from the database and constructs a `GradingSystem` object.
            * The `GradingSystem` class includes methods for validating the grading
              system (`isValid`) and sorting exam types for database
              insertion (`sortExamTypesForDatabaseInsertion`) to prevent foreign key constraint
              violations.
        * **Potential Issues:**  The database operations in `setGradingSystem` are not
          transactional. If one operation fails, the database could be left in an inconsistent
          state.
        * **Improvements:** Implement a database transaction to ensure that all operations
          in `setGradingSystem` are performed atomically.

    * **`API/supabase/grades/marks.dart`:**
        * **Functionality:** Manages the creation, retrieval, and calculation of marks for each
          user. Includes caching mechanisms to improve performance.
        * **Implementation Details:**
            * The `MarksCache` class provides a central cache for marks data. The `invalidateCache`
              function clears the cache, and the `isCacheValid` function checks if the cache is
              still valid.
            * The `_loadAllMarksData` function fetches all necessary data (grading system, marks,
              subjects) from the database and caches it. It uses `groupBy` from the `collection`
              package to organize marks by subject and exam type.
            * The `calculateAverageMarkForSubject` function calculates the average mark for a
              specific subject, taking into account the evaluation method (percentage or
              multiplication) and the weight of each exam type.
        * **Potential Issues:**  The cache expiration time (5 minutes) may not be optimal for all
          use cases. The `calculateAverageMarkForSubject` function can be complex and inefficient
          for large datasets.
        * **Improvements:**  Make the cache expiration time configurable. Optimize
          the `calculateAverageMarkForSubject` function by using more efficient data structures and
          algorithms.

    * **`main.dart` & Background Tasks:**
        * **Functionality:**  Initializes the app, schedules daily tasks for pre-lesson
          notifications.
        * **Implementation Details:**
            * Uses `AndroidAlarmManager` to schedule `scheduleTaskCallback` to run daily at
              midnight.
            * The `scheduleTaskCallback` function fetches the schedule and pre-lesson notification
              settings from Supabase and schedules local notifications
              using `flutterLocalNotificationsPlugin`.
        * **Potential Issues:** `AndroidAlarmManager` relies on the Android OS. Battery optimization
          settings can interfere with the execution of scheduled tasks.
        * **Improvements:**  Provide clear instructions to users on how to disable battery
          optimization for the app. Consider using Firebase Cloud Messaging (FCM) for more reliable
          push notifications (requires a server-side component).

* **Non-Obvious Implementation Strategies:**

    * **Database Insertion Order (Grading System):** The `sortExamTypesForDatabaseInsertion`
      function in `GradingSystem.dart` is crucial for ensuring that exam types are inserted into the
      database in the correct order, preventing foreign key constraint violations. This is a
      non-obvious but essential implementation detail.
    * **Marks Caching:** The `MarksCache` class in `marks.dart` implements a caching mechanism to
      reduce the number of database calls. This significantly improves the performance of the app,
      especially when displaying marks data.
    * **Color Encoding:** The `subjects.dart` file uses a hexadecimal string to store color values
      in the database. This is due to limitations in how Supabase handles the Flutter Color class.
      The code includes logic to convert between the Flutter Color class and the hexadecimal string
      representation.

**IV. Environment and Toolchain**

* **Development Environment:**

    * **Operating System:** macOS, Windows, or Linux
    * **IDE:** VS Code with Flutter extension, or Android Studio
    * **Flutter SDK:** Version [Specify version] (as defined in `fvm` if used, or Flutter version
      management).
    * **Dart SDK:** Version [Specify version] (comes with Flutter SDK).
    * **Dependencies:** Managed using `pubspec.yaml`.
    * **Supabase CLI:**  Used for managing the Supabase backend (database schema, functions, etc.).
    * **Emulator/Simulator:** Android Emulator or iOS Simulator for testing.
    * **Environment Variables:**
        * `SUPABASE_URL`: The URL of the Supabase project.
        * `SUPABASE_ANON_KEY`: The anonymous API key for the Supabase project.
        * `GEONAMES_API_USER`: The username for the GeoName API

* **Testing Environment:**

    * **Unit Tests:**  Using Flutter's built-in testing framework (`flutter test`).
    * **Integration Tests:**  Using Flutter's integration testing framework (`flutter drive`).
    * **Widget Tests:** Using Flutter's widget testing framework (`flutter test`).
    * **Mocking:** Using `mockito` or similar libraries for mocking dependencies in tests.

* **Production Environment:**

    * **Hosting:**  Supabase handles the hosting of the backend services (database, authentication).
    * **Deployment:**  Flutter app is built for Android and iOS and deployed to the respective app
      stores.
    * **Monitoring:**  Supabase provides basic monitoring tools. Consider integrating with a
      dedicated monitoring service (e.g., Sentry, Crashlytics) for more detailed error tracking and
      performance monitoring.

* **Toolchain:**

    * **Build System:** Flutter CLI (`flutter build apk`, `flutter build ios`).
    * **Deployment Pipeline:**  Manual deployment to app stores, or automated using CI/CD tools (
      e.g., GitHub Actions, Bitrise, Codemagic).
    * **Code Analysis:**  Dart analyzer (`dart analyze`).
    * **Formatting:**  Dart formatter (`dart format`).
    * **Linting:**  Linting rules defined in `analysis_options.yaml`.

**V. Critical Dependency Analysis**

* **External Libraries and Frameworks:**

    * `android_alarm_manager_plus`:  Scheduling background tasks on Android.
      Version [Specify version].
        * **Versioning:**  Follow semantic versioning.
        * **Patching Strategy:**  Monitor for updates and apply patches as needed.
        * **Known Issues:**  Battery optimization settings can interfere with the execution of
          scheduled tasks.
        * **Mitigation:** Provide clear instructions to users on how to disable battery
          optimization.
    * `flutter_local_notifications`:  Displaying local notifications. Version [Specify version].
        * **Versioning:**  Follow semantic versioning.
        * **Patching Strategy:**  Monitor for updates and apply patches as needed.
        * **Known Issues:**  Notification delivery may be delayed or suppressed on some Android
          devices.
        * **Mitigation:**  Test notification delivery on a variety of devices.
    * `supabase_flutter`: Flutter client for Supabase. Version [Specify version].
        * **Versioning:**  Follow semantic versioning.
        * **Patching Strategy:**  Monitor for updates and apply patches as needed.
        * **Known Issues:**  Potential issues with data serialization/deserialization.
        * **Mitigation:**  Use consistent data types and formats throughout the application.
    * `supabase_auth_ui`: UI components for Supabase Auth. Version [Specify version].
    * `logger`: Logging library. Version [Specify version].
    * `shared_preferences`:  Storing simple key-value data locally. Version [Specify version].
    * `http`: Making HTTP requests. Version [Specify version].
    * `intl`:  Internationalization and localization. Version [Specify version].
    * `collection`:  Utilities for working with collections. Version [Specify version].
    * `package_info_plus`: Accessing package metadata. Version [Specify version].
    * `lottie`: Render after-effects animations. Version [Specify version].
    * `flutter_colorpicker`: Color picker. Version [Specify version].
    * `flutter_svg`: Render SVG images. Version [Specify version].
    * `dotted_border`: Display dotted borders. Version [Specify version].
    * `pin_code_fields`: Display pin code fields. Version [Specify version].
    * `carousel_slider`: Display carousel sliders. Version [Specify version].

* **Dependency Management:**

    * `pubspec.yaml`:  Used to declare and manage dependencies.
    * `flutter pub get`:  Used to download and install dependencies.
    * `fvm`: Used to manage Flutter SDK versions.

* **Risks and Mitigation:**

    * **Dependency Conflicts:**  Potential conflicts between different versions of dependencies.
        * **Mitigation:**  Use a dependency management tool (e.g., `pubspec.lock`) to ensure that
          all dependencies are compatible.
    * **API Changes:**  External APIs may change, breaking the application.
        * **Mitigation:**  Monitor the APIs for changes and update the application accordingly. Use
          abstraction layers to minimize the impact of API changes.
    * **Security Vulnerabilities:**  Dependencies may contain security vulnerabilities.
        * **Mitigation:**  Regularly update dependencies to the latest versions. Use a vulnerability
          scanner to identify and address security vulnerabilities.

**VI. Performance Considerations**

* **Benchmarks:**  (Establish baseline benchmarks)

    * **App Startup Time:**  Measure the time it takes for the app to launch and display the initial
      screen.
    * **Data Fetching Time:**  Measure the time it takes to fetch data from the Supabase backend and
      external APIs.
    * **UI Rendering Time:**  Measure the time it takes to render complex UI components (e.g., the
      schedule grid).
    * **Memory Usage:**  Monitor the app's memory usage to identify potential memory leaks or
      excessive memory consumption.

* **Profiling Methods:**

    * **Flutter DevTools:**  Used for profiling CPU usage, memory allocation, and UI rendering
      performance.
    * **Android Profiler:**  Used for profiling Android-specific performance metrics.
    * **Instruments (iOS):** Used for profiling iOS-specific performance metrics.

* **Optimization Strategies:**

    * **Caching:**  Implement caching mechanisms to reduce the number of database calls and API
      requests. Use the `MarksCache` class as a starting point.
    * **Data Pagination:**  Implement pagination for large datasets to reduce the amount of data
      transferred over the network and improve UI rendering performance.
    * **Image Optimization:** Optimize images to reduce file size and improve loading times.
    * **Code Optimization:**  Identify and optimize inefficient code. Use the Flutter DevTools to
      profile CPU usage and memory allocation.
    * **Lazy Loading:** Load resources (e.g., images, data) only when they are needed.
    * **Asynchronous Operations:** Use asynchronous operations to avoid blocking the main thread and
      improve UI responsiveness.

* **Case Studies:**

    * **Past Performance Issues:
      **  [Document any past performance issues, their root cause, and the solutions implemented.]
    * **Example:** Slow data loading on the Grades page.
        * **Analysis:**  Identified that the `calculateAverageMarkForSubject` function was
          inefficient for large datasets.
        * **Solution:** Implemented caching and optimized the data structures