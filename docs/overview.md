> [!NOTE]
> Documentation auto-generated by [ai-docs.](https://github.com/connor-john/ai-docs)

```markdown
# SchoolMate: Technical Documentation

## Introduction:

SchoolMate is a mobile application designed to help students manage their academic life. It provides
features such as schedule management, homework tracking, grade calculation, and notifications. The
application is built using Flutter and utilizes Supabase for backend services, including
authentication and data storage.

- **Purpose:** To provide students with a comprehensive tool to organize and manage their
  school-related activities.
- **Core Functionality:** Managing schedules, tracking homework, calculating grades, and providing
  timely notifications.
- **Key Features:**
    - User Authentication: Secure sign-up and sign-in using Supabase Auth.
    - Schedule Management: Create, edit, and view class schedules.
    - Homework Tracking: Add, update, and track homework assignments.
    - Grade Calculation: Calculate and view grades based on a configurable grading system.
    - Notifications: Receive reminders for upcoming classes and homework deadlines.
    - Settings: Allows users to configure the application to their liking.
- **Potential Use Cases:**
    - Students can use SchoolMate to stay organized and on top of their coursework.
    - Teachers can integrate SchoolMate into their teaching workflow to communicate assignments and
      deadlines.
    - Parents can use SchoolMate to monitor their children's academic progress.

## Codebase Overview:

The SchoolMate codebase follows a modular architecture, separating concerns into distinct
directories and files. It leverages Flutter's component-based structure to build reusable UI
elements and Supabase's backend-as-a-service to handle data persistence and authentication.

- **Architecture:** The application follows a layered architecture consisting of UI components, API
  clients, data models, and utility functions.
- **Design Patterns:**
    - Singleton: Used for global instances like the Supabase client and logger.
    - Factory: Used for creating instances of data models from JSON responses.
    - Provider Pattern: Utilized by Flutter for state management, such as theming.
- **Modules and Components:**
    - `main.dart`: Entry point of the application, initializes Supabase, sets up daily task
      scheduling, and defines the main app widget.
    - `API/`: Contains API clients for interacting with external services and Supabase.
        - `externalAPIClients/`: Includes clients for fetching data from external APIs like
          restcountries and OpenHolidaysAPI.
        - `supabase/`: Contains modules for interacting with the Supabase backend.
            - `auth/`: Manages user authentication and settings.
            - `grades/`: Handles grading system and marks management.
            - `homeworks/`: Manages homework tasks.
            - `schedule/`: Manages schedule-related data and operations.
            - `settings/`: Manages user settings and notifications.
    - `Classes/`: Defines data models for various entities
      like `Country`, `Homework`, `Lesson`, `Mark`, etc.
    - `pages/`: Contains Flutter screens and UI components.
        - `home/`: Includes screens for the home page, homework, grades, and schedule.
        - `settings/`: Contains settings-related screens.
        - `userAuth/`: Manages user authentication flow.
    - `util/`: Includes utility functions for date manipulation, string formatting, and
      notifications.
    - `Widgets/`: Defines reusable Flutter widgets.

## Development Environment Setup:

To set up the development environment for SchoolMate, follow these steps:

1. **Install Flutter:**
    - Download and install the Flutter SDK from the official Flutter
      website: [https://flutter.dev/docs/get-started/install](https://flutter.dev/docs/get-started/install)
    - Configure your IDE to work with Flutter. The recommended IDEs are VS Code and Android Studio.
2. **Install Dependencies:**
    - Navigate to the project directory in the terminal.
    - Run `flutter pub get` to install the necessary Flutter packages.
3. **Install Supabase CLI:**
    - Follow the instructions on the Supabase website to install the Supabase
      CLI: [https://supabase.com/docs/guides/cli/getting-started](https://supabase.com/docs/guides/cli/getting-started)
4. **Configure Supabase:**
    - Initialize Supabase in your project directory: `supabase init`
    - Set up your Supabase project on the Supabase
      website: [https://supabase.com/](https://supabase.com/)
    - Configure the Supabase URL and anon key in `API/supabase/setup.dart`.
5. **Set up Android Alarm Manager Plus**
    - Follow the installation instructions on the package's
      website: [https://pub.dev/packages/android_alarm_manager_plus](https://pub.dev/packages/android_alarm_manager_plus)
6. **Environment Variables:**
    - Set up any required environment variables, such as the Geonames API username, as needed.

### IDE Configuration:

- **VS Code:**
    - Install the Flutter extension from the VS Code marketplace.
    - Configure the Flutter SDK path in VS Code settings.
- **Android Studio:**
    - Install the Flutter plugin from the Android Studio settings.
    - Configure the Flutter SDK path in Android Studio settings.

### Linters and Other Development Tools:

- **Linter:** Flutter uses Dart's built-in linter. Configure linting rules
  in `analysis_options.yaml`.
- **Formatter:** Use `dart format` to format your code according to Dart's style guide.
- **Version Control:** Use Git for version control and collaborate with others.

## Code Repository Structure:

```

school-mate/
├── API/
│ ├── externalAPIClients/
│ │ ├── geoAPIs.dart
│ │ └── OpenHolidaysAPI.dart
│ └── supabase/
│ ├── auth/
│ │ ├── userData.dart
│ │ └── userSettings.dart
│ ├── grades/
│ │ ├── gradingSystem.dart
│ │ └── marks.dart
│ ├── homeworks/
│ │ └── tasks.dart
│ ├── schedule/
│ │ ├── insertMetadata.dart
│ │ ├── lessons.dart
│ │ ├── schedule.dart
│ │ ├── subjects.dart
│ │ └── teachers.dart
│ └── settings/
│ └── preLessonNotifications.dart
├── Classes/
│ ├── geoPolitics/
│ │ ├── Country.dart
│ │ └── SubCountryState.dart
│ ├── homeworks/
│ │ └── Homework.dart
│ ├── marks/
│ │ ├── ExamType.dart
│ │ ├── GradingSystem.dart
│ │ └── Mark.dart
│ ├── persons/
│ │ ├── Gender.dart
│ │ └── Teacher.dart
│ └── schedule/
│ ├── Lesson.dart
│ ├── Schedule.dart
│ ├── ScheduleMetadata.dart
│ ├── SchoolHoliday.dart
│ └── Subject.dart
├── pages/
│ ├── home/
│ │ ├── home/
│ │ │ ├── start.dart
│ │ │ └── Widgets/
│ │ │ ├── DayProgressBar.dart
│ │ │ ├── HomeDrawer.dart
│ │ │ └── UpcomingHolidaysCard.dart
│ │ ├── homework/
│ │ │ ├── Homework.dart
│ │ │ ├── add/
│ │ │ │ ├── AddHomework.dart
│ │ │ │ └── Widgets/
│ │ │ │ ├── AdditionalHomeworkNote.dart
│ │ │ │ ├── DateSelector.dart
│ │ │ │ ├── HandInToggle.dart
│ │ │ │ ├── SubjectChooser.dart
│ │ │ │ └── TitleSelector.dart
│ │ │ └── Widgets/
│ │ │ └── HomeworkBox.dart
│ │ ├── marks/
│ │ │ ├── Grades.dart
│ │ │ ├── MarkSelection.dart
│ │ │ ├── Utils.dart
│ │ │ ├── add/
│ │ │ │ ├── AddMark.dart
│ │ │ │ └── subpages/
│ │ │ │ ├── subject.dart
│ │ │ │ ├── type.dart
│ │ │ │ └── validation.dart
│ │ │ ├── overview/
│ │ │ │ ├── EmptySubjectsNotics.dart
│ │ │ │ ├── MarksOverviewPage.dart
│ │ │ │ ├── MarkSubjectCard.dart
│ │ │ │ ├── SkeletonLoader.dart
│ │ │ │ └── subjectView/
│ │ │ │ └── SubjectMarksInspectionPage.dart
│ │ ├── schedule/
│ │ │ ├── lessons/
│ │ │ │ └── ConfigureLesson.dart
│ │ │ ├── page/
│ │ │ │ ├── Schedule.dart
│ │ │ │ └── Widgets/
│ │ │ │ ├── LessonBox.dart
│ │ │ │ └── ScheduleGridView.dart
│ │ │ ├── setup/
│ │ │ │ ├── scheduleSetup.dart
│ │ │ │ └── Widgets/
│ │ │ │ ├── AlternatingWeeksSelector.dart
│ │ │ │ ├── IndividualLessonDurationSelector.dart
│ │ │ │ ├── LessonsTimeFrame.dart
│ │ │ │ └── WorkdaySelector.dart
│ │ │ └── subjects/
│ │ │ ├── SubjectCreation.dart
│ │ │ ├── SubjectListWidget.dart
│ │ │ └── SubjectsListPage.dart
│ │ └── Widgets/
│ │ └── BottomNavBar.dart
│ ├── settings/
│ │ ├── notifications/
│ │ │ └── PreLessonNotificationSetup.dart
│ │ ├── setup.dart
│ │ └── Widgets/
│ │ └── settings/
│ │ └── ResidenceSelector/
│ │ ├── CountrySelector.dart
│ │ └── StateSelector.dart
│ └── userAuth/
│ ├── authenticationFlow.dart
│ ├── emailVerification.dart
│ └── userAuthentication.dart
├── Widgets/
│ ├── public/
│ │ ├── ConnectionTimedOut.dart
│ │ ├── GradientButton.dart
│ │ ├── GradientCircleAvatar.dart
│ │ ├── GradientSlider.dart
│ │ ├── MultipleStepPageIndicator.dart
│ │ ├── PreviousPage.dart
│ │ ├── ShimmerEffectForSkeletonLoader.dart
│ │ └── TimePicker.dart
├── main.dart
├── util/
│ ├── alphabet.dart
│ ├── dates.dart
│ ├── NavigatorTree.dart
│ └── notifications/
│ ├── debug.dart
│ └── schedule.dart
└── ...

```

-   **Naming Conventions:**
    -   Dart files: Use snake\_case (e.g., `user_data.dart`).
    -   Classes: Use PascalCase (e.g., `UserData`).
    -   Variables and functions: Use camelCase (e.g., `getUserData`).
-   **File Organization:**
    -   Separate files for each class, widget, or function.
    -   Group related files into directories based on functionality.
-   **Specific Standards:**
    -   Use `logger` for logging errors, warnings, and information.
    -   Handle asynchronous operations using `async` and `await`.
    -   Use `FutureBuilder` to handle asynchronous data in UI components.
    -   Utilize `setState()` to update the UI and trigger rebuilds.
    -   Follow Flutter's recommended practices for state management.

## Key Points of Complexity:

1.  **Schedule Management and Notifications:**
    -   **Complexity:** The interaction between schedule data, local notifications, and background tasks requires careful coordination.
    -   **Breakdown:**
        -   **Daily Task Scheduling:** The `scheduleDailyTask()` function in `main.dart` uses `AndroidAlarmManager` to schedule a task that runs at midnight. This task is responsible for scheduling pre-lesson notifications for the current day.
        -   **Notification Scheduling:** The `schedulePreLessonNotificationsForCurrentDay()` function in `util/notifications/schedule.dart` fetches the user's schedule and pre-lesson notification settings. It then schedules local notifications for each lesson based on the user's preferences.
        -   **Time Zone Handling:** The application uses the `timezone` package to handle time zone conversions and ensure that notifications are triggered at the correct time, regardless of the user's location.
        -   **Permission Management:** The application requests the `scheduleExactAlarm` permission from the user to ensure that the scheduled tasks can run accurately.
        -   **Error Handling:** The `scheduleTaskCallback()` function includes error handling and retry logic to ensure that the scheduled task is executed successfully, even if there are temporary issues with the Supabase connection.
2.  **Grading System and Marks Calculation:**
    -   **Complexity:** The application supports configurable grading systems, including percentage-based and multiplication-based evaluation methods.
    -   **Breakdown:**
        -   **Data Model:** The `GradingSystem` class in `Classes/marks/GradingSystem.dart` defines the structure of a grading system, including the range of possible marks, modifiers, and exam types.
        -   **Evaluation Methods:** The `EvaluationMethod` enum in `Classes/marks/ExamType.dart` defines the different evaluation methods that can be used in the grading system.
        -   **Database Storage:** The grading system and exam types are stored in the Supabase database using a hierarchical structure. The `exam_types` table stores the details of each exam type, including its name, multiplication factor, and percentage. The `grading_systems` table stores the details of the grading system, including the range of possible marks and modifiers.
        -   **Calculation Logic:** The application uses the `calculateAverageMarkForSubject()` function in `API/supabase/grades/marks.dart` to calculate the average mark for a specific subject. This function takes into account the evaluation method of each exam type and applies the appropriate calculation logic.
        -   **Circular Dependency Detection:** The application includes a circular dependency check to prevent multiplication-based exam types from forming a circular pattern. This is done using the `multiplicationExamTypesCircularPatternCheck()` function in `Classes/marks/GradingSystem.dart`.
3.  **Data Caching:**
    -   **Complexity:** The application uses a caching mechanism to reduce the number of database queries and improve performance.
    -   **Breakdown:**
        -   **MarksCache Class:** The `MarksCache` class in `API/supabase/grades/marks.dart` stores the cached data.
        -   **Cache Invalidation:** The `invalidateCache()` function in `MarksCache` clears the cache to force a refresh on the next fetch.
        -   **Cache Validation:** The `isCacheValid()` function in `MarksCache` checks if the cache is still valid (less than 5 minutes old).
        -   **Data Loading:** The `_loadAllMarksData()` function in `API/supabase/grades/marks.dart` loads all necessary data once and stores it in the cache.

## Installation and Setup:

1.  **Prerequisites:**
    -   Flutter SDK
    -   Dart SDK
    -   Supabase CLI
    -   Android SDK (if developing for Android)
    -   Xcode (if developing for iOS)
2.  **Clone the Repository:**
    ```bash
    git clone <repository_url>
    cd <project_directory>
    ```
3.  **Install Dependencies:**
    ```bash
    flutter pub get
    ```
4.  **Configure Supabase:**
    -   Run `supabase init` to initialize Supabase in your project.
    -   Set up a Supabase project on the Supabase website.
    -   Configure the Supabase URL and anon key in `API/supabase/setup.dart`.
5.  **Run the Application:**
    ```bash
    flutter run
    ```

## Getting Started:

1.  **Initial Setup:**
    -   After installing the application, sign up for a new account or sign in with an existing account.
    -   Complete the initial setup process by providing your residence and other settings.
2.  **Quick Start:**
    -   Navigate to the Schedule tab and create a new schedule.
    -   Add a few subjects to your schedule.
    -   Navigate to the Homework tab and add a homework assignment.
    -   Navigate to the Grades tab and set up a grading system.
    -   Add a few marks to your grades.
3.  **Basic Examples:**
    -   **Adding a Lesson:**
        1.  Navigate to the Schedule tab.
        2.  Tap the "+" button to add a new lesson.
        3.  Select the subject, day, time, and other details.
        4.  Save the lesson.
    -   **Tracking Homework:**
        1.  Navigate to the Homework tab.
        2.  Tap the "+" button to add a new homework assignment.
        3.  Enter the title, subject, due date, and other details.
        4.  Save the homework assignment.
    -   **Calculating Grades:**
        1.  Navigate to the Grades tab.
        2.  Set up a grading system by defining the range of possible marks, modifiers, and exam types.
        3.  Add marks for each subject and exam type.
        4.  View the calculated grades.
```